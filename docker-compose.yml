services:
  wss:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5555:5555"
    environment:
      - NODE_ENV=production
      - ORIGIN=https://undercover-drab.vercel.app
